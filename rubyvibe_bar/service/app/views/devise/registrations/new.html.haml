%section.auth-card.auth-card--narrow.tavern-card
  %h1.auth-title Регистрация в таверне
  %p.auth-meta Присоединяйтесь к охоте за неоновыми квестами.

  = form_for(resource, as: resource_name, url: registration_path(resource_name), html: { class: "form" }) do |f|
    = render "devise/shared/error_messages", resource: resource

    .form__field
      = f.label :username, "Username", class: "form__label"
      = f.text_field :email, autofocus: true, autocomplete: "username", class: "form__input"

    .form__field
      = f.label :password, "Пароль", class: "form__label"
      = f.password_field :password, autocomplete: "new-password", class: "form__input"

    .form__field
      = f.label :password_confirmation, "Подтверждение пароля", class: "form__label"
      = f.password_field :password_confirmation, autocomplete: "new-password", class: "form__input"

    = f.submit "Зарегистрироваться", class: "btn btn--primary btn--block"

  %p.auth-meta
    Уже в таверне?
    = link_to "Войти", new_session_path(resource_name)
